#ifndef size
#define size(arr) sizeof(arr)/sizeof(arr[0])
#endif

#include <iostream>

int kmp_search(std::string str, std::string substr) {
    
    return 0;
}

int main() {
    kmp_search("abracadabra", "braca");
    return 0;
}
